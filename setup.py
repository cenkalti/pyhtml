from setuptools import setup

setup(
    name = 'pyhtml',
    version = '0.1',
    author = 'Cenk Alti',
    description = 'Python HTML generator.',
    license = 'BSD',
    packages = ['pyhtml'],
    test_suite = 'tests',
)
